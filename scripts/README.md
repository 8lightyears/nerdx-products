# Scripts

Reusable generators that turn an idea into artwork or raw material. Each script
carries a short run/verify note at the top (dependencies + one example command).

Planned:
- equation typesetting: LaTeX / Matplotlib -> clean transparent PNG/SVG
- fractal renderer (e.g. Mandelbrot/Julia) at print resolution
- public-domain image fetcher (with source + license capture)

(No scripts yet.)
